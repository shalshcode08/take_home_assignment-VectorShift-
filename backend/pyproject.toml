[project]
name = "vectorshift-backend"
version = "0.1.0"
description = "Pipeline-parsing backend for the VectorShift frontend assessment"
requires-python = ">=3.10"
dependencies = [
  "fastapi",
  "uvicorn",
]

[tool.uv]
package = false
